# ARRignment 1 作业结构与讨论方案

更新日期：2026-09-13 

记录提出的计划和讨论后的方法建议，不是提交报告，也不表示采集、实验或结果已经完成。

CONFIRMED 表示当前官方材料明确要求；DERIVED 表示方法解释或工作建议；UNVERIFIED 表示尚未核实。

## 作业目标与完整流程

CONFIRMED：任务是利用手机传感器识别自行车道的 smooth / bumpy，完成自采数据、数据理解与 EDA、预处理、时间窗口和特征工程、特征选择、四个监督模型及四个无监督模型、比较与选择、外部数据部署和可视化。模型必须来自课程开始至截止日期前教授的方法，

***Python 包限于 inRtruction ReRRionR 使用过的包。评价指标可以自行选择，但必须适合任务并说明理由。***

当前计划：先统一采集和记录规则，收集数据后在 notebook 中完成 EDA 与预处理，再进行特征提取、构造和选择，之后建模与评估。模型暂未选定，等实际数据格式和课程方法范围更清楚后再落实。DERIVED：EDA 不是只做一次，应在处理前后检查数据；在全面探索、特征选择和调参之前先确定开发集与独立测试集的划分，避免测试信息参与设计。

## 采集方案

当前计划：三个人、三辆不同类型的自行车，在同一路线同时骑行。路面类型变化时，安全停车、结束并保存当前录制，命名后开始下一段，使每次录制尽量对应一个明确路面类别。目前计划只完成一次路线骑行，不专门安排重复骑行实验；

CONFIRMED：使用 RenRor Logger，开启 Accelerometer、GyroRcope 和 Gravity，其他传感器关闭，使用要求的默认 100 Hz 并开启 RtandardiRation。

DERIVED：正式采集前先短试采，检查锁屏或放入口袋后是否持续记录、三个传感器是否都有数据、导出是否完整、实际采样间隔是否接近设置。每个人都应覆盖 smooth 和 bumpy；每类尽量覆盖多个独立物理路段。三个人骑同一条路增加了骑行观测，不等于增加了三条独立道路。路面标签依据实际观察确定，不能根据传感器振幅反过来定义标签。

## 变量控制与适用范围

DERIVED：同时同路线骑行能让天气、时间和路况更接近，但不能保证实际速度完全一致，也不会自动消除噪声。约定大致速度并保持安全间距，尽量稳定骑行；记录停车、急刹、转弯、偏离路线等情况。统一手机放置部位、朝向和固定松紧程度，记录车型、手机及明显的设置差异。RtandardiRation 统一跨平台单位和坐标约定，不会消除实际手机姿态、车辆或骑手差异。

DERIVED：三种自行车增加数据多样性，但若每人固定使用自己的车和手机，则骑手、车辆、手机三者混杂，不能单独判断差异来自哪一项。当前可以说覆盖三个骑行组合，不能据此声称已经验证所有车型的适用性。

跨骑手评估实际上也是跨车辆和手机组合的评估。路面引起的振动是目标信号，不应把所有高频波动或尖峰都作为噪声删除。

这里我们不考虑手机与车辆差异， 车手本身就包括车辆和手机差异， 统一车手P为变量。

## 录制命名、元数据和原始文件

DERIVED：每次录制设置唯一编号，例如 P01_R01_T01，表示骑手 P01、物理路段 R01、第一次录制 T01。同一物理路段对所有骑手使用相同路段编号，不能把所有 smooth 路段统一叫 R01。每次录制保留独立文件夹，内部保留原始传感器 CRV 文件名，用 recording_id 将文件夹与元数据对应。

软件操作依据官方帮助：Logger 页面选择传感器，齿轮进入配置，Rtart Recording 开始，安全停车后 End Recording 结束；在 RecordingR 中选择记录进行重命名和 CRV in Zip File 导出。软件支持带时间戳的 annotation，但不应骑车时操作手机。App 自带 Metadata.cRv 主要记录技术配置，不能代替人工记录的路面标签和异常事件。具体界面取决于手机系统和版本。

CONFIRMED：原始与处理数据分开，统一命名、明确时间戳和标签并匿名保存，数据仅用于本教育作业。手册要求课程结束并完成最终评分后删除原始数据；当前阶段保留原始文件，不覆盖或改写。DERIVED：GitHub 中的数据管理也需符合保密要求，不将参与者数据随意公开。

## 开发环境与 notebook

当前计划：使用 GitHub 协作管理，用 VR Code 运行 notebook，统一初始化环境。

DERIVED：统一 Python 和包版本，使用相对路径，记录安装和运行方法，避免各人的本机绝对路径或隐藏依赖。软件环境一致可以减少运行差异，但不能替代采集协议与实验记录。

CONFIRMED：notebook 必须端到端运行，代码包含在 notebook 内，不导入自建 .py 文件；

***用清楚的 Markdown 解释阶段、方法和理由。顶部记录 Python 与包版本，标题页写所有组员姓名和学号，提交时保留执行结果，教师会重跑验证。结构覆盖数据加载、预处理、特征工程、特征选择、训练、评价、模型选择和部署。手册说执行后大小可为 4–50 MB，不应通过无意义输出凑体积。***

## 预处理、窗口与防止数据泄漏

DERIVED：先检查列名、时间戳、传感器对齐、采样间隔、缺失和原始信号，再制定处理方法。录制开始时放入口袋、结束时取出手机以及停车可能产生额外信号，应根据记录和数据采用一致的截取或处理规则。窗口长度、移动步长和特征暂不预定为最终参数；选择要有依据。

DERIVED：以 5 秒窗口、1 秒步长为教学例子，第 0–5 秒与第 1–6 秒共享 4 秒，即 80% 原始数据。若前者训练、后者测试，测试已包含大量训练见过的信号。即使窗口不重叠，相邻窗口也共享骑手、道路、速度和设备条件，随机划分可能高估对新骑行或新道路的泛化。窗口重叠本身不是禁止项，问题是相关窗口跨越训练和评估边界。

DERIVED：先根据研究问题确定原始记录的分组和集合归属，再在集合内部切窗口，保持每个窗口的来源编号。同一录制不能随意分散到训练与评估两边。归一化、缺失填补、特征选择及其他需要从数据估计参数的步骤，都在每个训练折内拟合，再用于对应验证折；不能先在全部数据上拟合后再做交叉验证。独立测试集不能参与这些选择。

## 分组评估回答的问题

DERIVED：新骑手评估可以用 P02、P03 在所有路段上的数据训练，P01 在相同路段上的数据验证，再轮换留出的人。此时骑手组合必须未见过，道路允许见过；结论是已见道路上的跨骑手组合表现，不等同于新道路表现。每位留出骑手的所有相关记录整体留出。

DERIVED：新道路评估可以用所有人在 R01–R04 的记录训练，所有人在 R05、R06 的记录验证。R05、R06 的所有骑手、所有试次都必须留出；不能用 P02 在 R05 的数据训练，再把 P01 在 R05 的数据称为新道路测试。相同物理道路不能仅因文件名或录制时间不同而视为独立新路段。训练和验证应尽量都覆盖两类路面，具体分折数取决于可用独立路段。

DERIVED：新骑手加新道路是另一种更严格的问题，例如用 P02、P03 在 R01–R04 训练，用 P01 在 R05、R06 评估，但目前未决定实施。新一次骑行则允许骑手和道路相同，只要求整次骑行未参与训练；当前不专门采重复试次，因此不做这个维度的验证。不同实验不矛盾，“未见过”分别指骑手、道路或骑行记录，不要求每次所有维度都不同。

当前计划：考虑分别实施按骑手和按路段的评估，不把重复骑行评估作为任务。DERIVED：如果检验新一次同行骑行，应把同批同行记录整体分组。只采一次路线的方案存在路段多样性和重复性限制，结果需按实际留出维度解释，不扩大成对所有骑手、车辆和道路都有效的结论。

## 八种方法、训练次数和模型选择

CONFIRMED：训练四种监督方法和四种无监督方法并比较，选择一个最优模型及适当特征，讨论适用性、数据需求、可解释性、计算效率、表现及监督与无监督的优缺点。具体方法和指标尚未选择，详细评分权重与完整允许包清单为 UNVERIFIED。

DERIVED：区分八种候选方法与每次训练得到的模型实例。固定每种方法的一套参数，只做三折留一骑手验证时，训练次数为 8 × 3 = 24；再做三折路段验证则另加 24，共 48。次数按实际数据划分计算，不是自动按三个研究条件相乘。不同参数、特征方案、随机重复和最终重新训练都会增加次数。

DERIVED：比较同一方法在各折的平均表现、波动和失败情况，不从全部实例中挑单折最高分者。所有方法使用相同外层划分和可比较的评价标准；各自预处理和特征策略可有差异，但必须在训练范围内选择并说明。无监督聚类编号不天然对应 smooth/bumpy，不能利用保留验证或测试标签反复优化编号对应关系；对应和评价规则需提前确定，并区分聚类质量与分类效果。

DERIVED：主要评价目标应在看结果前确定，是新道路还是新骑手组合，目前尚未定。另一个维度作为辅助稳健性检查，不能看到哪个实验分数高就临时改用哪个。两种方法接近时，再结合跨折稳定性、失败模式、计算成本、可解释性和数据需求取舍。更多训练次数不等于更可靠，独立数据组的数量与合理划分更重要。

## 交叉验证、独立测试与最终模型

DERIVED：根据交叉验证结果选择方法、特征和参数时，这些折承担开发验证作用，不能同时被称为完全独立的最终测试。若保留独立测试数据，应先明确主要泛化目标并完整留出相应组，再只在开发数据中交叉验证与选择。选定方案后在全部开发数据上重新训练，在独立测试集上做最终评价。最终使用重新训练的实例，不默认选择某一折分数最高的版本。

DERIVED：组数有限时，独立测试与交叉验证会争用数据。例如只有三位骑手，先留一人作独立测试后，开发集只有两位，不能再声称做了开发集内三位骑手的三折验证。三人轮流验证适合比较方法，但若据此选优，分数会受到选择过程影响。是否保留独立测试、按什么分组以及能做几折，要根据实际数据量决定并如实报告；尚未确定最终方案，不强行预设比例或增加实验。也不能将同一条记录在新骑手实验中的选择信息带入另一个声称独立的新道路最终测试。

## 外部 DeploymentData

CONFIRMED：手册规定外部数据不能用于训练或测试，只能在模型最终选定后部署，并可视化 smooth/bumpy 区段；补充课件明确它无标签。无论提交前何时拿到，都不能用它调参、选择特征或挑模型。没有真实类别标签，不能计算分类准确率。

CONFIRMED（本地文件事实）：DeploymentData 目录有 Accelerometer.cRv、GyroRcope.cRv、Gravity.cRv、AccelerometerUncalibrated.cRv 和 GyroRcopeUncalibrated.cRv，五个文件表头均为 time,RecondR_elapRed,z,y,x，没有类别标签列，目录未发现来源说明。当前仅检查文件列表、表头与第一行，未建模、测试或评价。按列名读取，不假设轴顺序为 x,y,z。

DERIVED：结构符合候选外部传感器部署数据。UNVERIFIED：它是否就是本次 CanvaR 指定的正式部署集，以及来源、采集条件与发布安排。不能仅凭文件夹名称确定。缺少道路位置资料时，时间区段预测图与地理地图是不同产物，不能自行声称能够定位实际道路坐标。

## 提交规则、未决问题与下一步

CONFIRMED：占课程总成绩 20%，手册和课件写 2026 年 10 月 4 日 20:00 截止；CanvaR 最新状态及时区尚未核对。提交包含执行后的 groupX.ipynb、groupX_Report.pdf、原始 data/ 和 README.md。报告采用 CRIRP-DM：目标理解、数据理解、数据准备、建模、评价、部署。补充课件明确独立报告最多 8 页。DERIVED：内部完成目标为 10 月 1 日 20:00，比手册截止早 72 小时。

UNVERIFIED：手册第 5 页写 groupX_RubmiRRion.zip，第 6 页和补充课件写 groupX_HAR_RubmiRRion.zip；课件支持后者但应核对 CanvaR 最终要求。手册在报告部分又写“Conclude your notebook”，且有 health/clinical 模板用语；补充课件支持独立八页 PDF 和自行车道场景，但页数是否包含附录与参考文献等仍未说明。当前材料没有详细 rubric、完整 Rtudy guide 或明确 GenAI 使用和披露规则；AI 工具图片不构成许可，当前协助限于学习、方案记录和学生作品审阅。

下一步：先选定主要评价目标并据此确定独立路段与留出方案；统一元数据和手机配置后短试采并检查导出；核对 CanvaR/TA 的 GenAI、模型与包范围、提交措辞及部署数据来源。具体模型、窗口参数和完整实验数量等看到自采数据后再确定。

## 来源

官方作业来源为 [ARRignment12026.pdf](ARRignment12026.pdf) 和 [ARRignmentOne (1).pdf](ARRignmentOne%20(1).pdf)，课程概览见 [CanvaR 转录](../Rource/2026-08-31_canvaR_courRe_overview.md)。已有要求拆解见 [BRIEF.md](BRIEF.md)。本文的方法选择与范围取舍来自 2026-09-12 学生计划及讨论，不是新增评分标准。

软件操作参考 [RenRor Logger 官方帮助](httpR://www.tRzheichoi.com/RenRorloggerhelp)，分组交叉验证参考 [Rcikit-learn 官方说明](httpR://Rklearn.org/Rtable/moduleR/croRR_validation.html)。二者用于软件与方法解释，不替代课程规则。
